---
name: Feature request
about: Suggest a new command, flag, or capability for ohnoo
title: "[feature] "
labels: ["enhancement"]
---

## What problem does this solve?

<!-- What are you trying to do that ohnoo can't do today? Describe the
     situation, not just the feature — "I want X" is less useful than
     "when Y happens, I have to do Z manually." -->

## Proposed solution

<!-- What would the command/flag/behavior look like? A rough sketch of the
     CLI invocation or output is more useful than prose. -->

```
$ ohnoo ...
```

## Alternatives considered

<!-- Have you found a workaround? Is there a reason the existing commands
     (ohnoo explain, ohnoo fix, ohnoo setup-ai, etc.) don't cover this? -->

## Which layer does this touch?

<!-- Layer 1 (offline patterns), Layer 2 (agent handoff), Layer 3 (MCP),
     Layer 4 (hosted fallback), or something else entirely (--vibe, --share,
     --stats, shell hooks)? See CLAUDE.md for the architecture if unsure. -->
